const pyramid = (n) => {
  let addOn = 0;
  for(let i=1; i<=n; i++){    //1,2,3
    res= "";
    res+= " ".repeat(n-i);
    res+= "#".repeat(i + addOn); //1+0 ,  2+1, 3+2
    res+= " ".repeat(n-i);
    addOn +=1    // addon now 1,2,3
    console.log(res)
  }
}

pyramid(10)