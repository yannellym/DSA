function chunk(arr, num){
  let arrStorage = [];
  let index = 0;
  while( index < arr.length ){
    arrStorage.push(arr.slice(index, index + num))
    index += num;
  }
  console.log(arrStorage)
}

chunk([1,2,3,4,5,6,7,8],4)
