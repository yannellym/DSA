function fizzBuzz(n){
  for(let i=0; i <=n; i++){
    let str = "";
   
    if(i % 3 === 0){
      str += "Fizz" 
    }
    if(i % 5 === 0 ){
      str += "Buzz"
    }
    if( str === ""){
      str = i
    }
      console.log(str)
  }
  
}

fizzBuzz(100)
